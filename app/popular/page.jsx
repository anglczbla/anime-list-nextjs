import HeaderMenu from "../ui/HeaderMenu";
import Pagination from "../ui/Pagination";

const page = () => {
  return (
    <div>
      <HeaderMenu />
      <Pagination />
    </div>
  );
};

export default page;
